import { RouterConfig } from '@angular/router';
import {SalaryComponent} from './salary.component'
export const SalaryRoute: RouterConfig = [
    {
        path: 'salary',
        component: SalaryComponent
    }
]
