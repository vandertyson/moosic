import { RouterConfig } from '@angular/router';
import { UserAddComponent } from './user-add.component'

export const UserAddRoutes: RouterConfig = [
  {
    path: "add",
    component: UserAddComponent,
  }
]
