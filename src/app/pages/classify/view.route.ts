import { RouterConfig } from '@angular/router';
import { ClassifyComponent } from './view.component'

export const ClassifyRoutes: RouterConfig = [
    {
        path: 'class',
        component: ClassifyComponent
    }
]
