import { RouterConfig } from '@angular/router';
import { Profile } from './profile.component'
export const ProfileRoute: RouterConfig = [
    {
        path: 'profile',
        component: Profile
    }
]
